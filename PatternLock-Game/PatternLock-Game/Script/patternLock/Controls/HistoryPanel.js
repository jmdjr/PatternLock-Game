define(function () {
    return {
        //images for display (border box and button for attempt #)
        //collection of attempted path
        // can return a list of GameNodes
        historyList: [],
    }
});